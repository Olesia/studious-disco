import { BgChangerDirective } from './bg-changer.directive';

describe('BgChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new BgChangerDirective();
    expect(directive).toBeTruthy();
  });
});
